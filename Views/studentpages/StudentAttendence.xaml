<Page x:Class="AttendenceManagementSystem.Views.studentpages.StudentAttendence"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006" 
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008" 
      xmlns:local="clr-namespace:AttendenceManagementSystem.Views.studentpages"
      mc:Ignorable="d" 
      d:DesignHeight="600" d:DesignWidth="800"
      Title="StudentAttendence"
      Background = "Transparent"
      MouseDown="StudentAttendencepage_MouseDown"
       >
    


    <Grid Margin="20" ShowGridLines="False">
        <Grid.RowDefinitions>
            <RowDefinition Height="0.2*" />
            <RowDefinition Height="0.4*" />
            <RowDefinition Height="1.5*" />
        </Grid.RowDefinitions>

        <!-- Search and Filter Section -->
        <DockPanel Grid.Row="1" Margin="20" VerticalAlignment="Center" HorizontalAlignment="Stretch">
            <StackPanel HorizontalAlignment="Right" VerticalAlignment="Center" Width="Auto" DockPanel.Dock="Right">

                <DatePicker x:Name="AttendanceDatePicker"   Height="30" MinWidth="150" Margin="5" SelectedDateChanged="OnDateChanged" HorizontalAlignment="Stretch" >
                    <DatePicker.Resources>
                        <Style TargetType="DatePickerTextBox">
                            <Setter Property="IsReadOnly" Value="True" />
                        </Style>
                    </DatePicker.Resources>
                </DatePicker>
            </StackPanel>
            <StackPanel HorizontalAlignment="Left" VerticalAlignment="Center" Width="Auto" DockPanel.Dock="Left">
                <ComboBox x:Name="CourseComboBox" Height="30" MinWidth="150" 
                          Margin="5" HorizontalAlignment="Stretch" SelectionChanged="OnCourseChanged"
                          >
                  
                </ComboBox>
            </StackPanel>
        </DockPanel>

        <!--<ScrollViewer Grid.Row="2" Margin="10" HorizontalScrollBarVisibility="Auto" VerticalScrollBarVisibility="Auto">-->
        <DataGrid x:Name="AttendanceDataGrid" 
                      AutoGenerateColumns="False"
                      RowHeaderWidth="0"
                      VerticalGridLinesBrush="Transparent" 
                      HorizontalGridLinesBrush="Transparent"
                      Background="Transparent" 
                      BorderBrush="Transparent" 
                      BorderThickness="2"
                      CanUserResizeColumns="False"
                      CanUserResizeRows="False"
                       RowHeight="30"
                       ColumnHeaderHeight="30"
                      CanUserDeleteRows="False"
                      CanUserAddRows="False"
                      CanUserReorderColumns="False"
                      IsReadOnly="True"
                      RowStyle="{StaticResource DataGridRowStyle}"
                      ColumnHeaderStyle="{StaticResource DataGridColumnHeaderStyle}" Grid.Row="2" Margin="10" Width="384"
                      VerticalScrollBarVisibility="Auto" HorizontalScrollBarVisibility="Auto"
                      >


            <!-- Column Definitions -->
            <DataGrid.Columns >
                <DataGridTextColumn Header="Date"  Binding="{Binding Date}" Width="*"  MaxWidth="100" MinWidth="100" >
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextAlignment" Value="Center" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
                <DataGridTextColumn Header="Course" Binding="{Binding Course}" Width="*" MaxWidth="1500"  MinWidth="150">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextAlignment" Value="Center" />
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>

                <DataGridTextColumn Header="Status" Binding="{Binding Status}" Width="*" MaxWidth="100"  MinWidth="100">
                    <DataGridTextColumn.ElementStyle>
                        <Style TargetType="TextBlock">
                            <Setter Property="TextAlignment" Value="Center" />
                            <Style.Triggers>
                                <DataTrigger Binding="{Binding Status}" Value="Absent">
                                    <Setter Property="Foreground" Value="Red" />
                                </DataTrigger>
                            </Style.Triggers>
                        </Style>
                    </DataGridTextColumn.ElementStyle>
                </DataGridTextColumn>
            </DataGrid.Columns>
        </DataGrid>
        <!--</ScrollViewer>-->
    </Grid>
</Page>
